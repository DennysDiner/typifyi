# typifyi

simple typing practice

## Projects in this repository

| Path | What it is |
| --- | --- |
| `index.html`, `main.js`, `styles.css` | the typing practice page |
| `tweet_parser_sampler.py` | tweet archive parser and sampler ([notes](TWEET_PARSER_README.md)) |
| [`tas-notice-watcher/`](tas-notice-watcher/) | change detection for Tasmanian government publication surfaces — Gazette, tenders, contract disclosures and the Register of Lobbyists — alerting through GitHub Issues ([README](tas-notice-watcher/README.md), [runbook](tas-notice-watcher/docs/OPERATIONS.md)) |

`tas-notice-watcher` runs on the schedules in `.github/workflows/tas-notice-watcher*.yml`
and keeps its state, records and archive inside its own directory.
